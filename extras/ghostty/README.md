# Ghostty themes

To use, place the themes in your `~/.config/ghostty/themes/` folder and use the
following configuration option:

```conf
# for darker, for example
theme = light:odenwald_light,dark:odenwald_darker
```
